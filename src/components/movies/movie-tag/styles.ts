
import { styled } from "@mui/material";

export const MovieTagContainer = styled('div')(({ theme }) => ({
    color: '#fff',
    padding: '4px',
    borderRadius: '8px',
    backgroundColor: '#000'
}));
