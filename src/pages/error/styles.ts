import { Stack, styled } from "@mui/material";

export const ErrorPageContainer = styled(Stack)(({ theme }) => ({
  width: "100%",
  height: "100%",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
}));
